// @flow
import React from 'react';

import LoginContainer from '../../containers/Login';

const Login = () => (
  <div>
    <h1>Login</h1>
    Login one more time!
    <LoginContainer />
  </div>
);

export default Login;
